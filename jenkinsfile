node {
    stage ('clone') {
        checkout scm 
    }
    
    stage ('docker build') {
            dir('backend/chatting-service'){
                sh 'pwd'
                sh 'ls'
                app = docker.build("hyuna919/jenkins:$BUILD_NUMBER")
            }
    }
    
}
