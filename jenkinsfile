node {
    stage ('clone') {
        checkout scm 
    }
    
    stage ('docker build') {
        echo 'pwd'
        echo 'ls'
        app = docker.build("hyuna919/jenkins:$BUILD_NUMBER")
    }
    
}
